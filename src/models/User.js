// User model
class User {
  constructor(id, name, email) {
    this.id = id;
    this.name = name;
    this.email = email;
  }

  static findById(id) {
    // Implementation
  }

  static findAll() {
    // Implementation
  }
}

module.exports = User;
